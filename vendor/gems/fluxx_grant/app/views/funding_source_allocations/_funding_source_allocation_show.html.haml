= model.funding_source.name if model.funding_source
= model.program.name if model.program
= model.sub_program.name if model.sub_program
= model.initiative.name if model.initiative
= model.sub_initiative.name if model.sub_initiative
= model.authority if model.sub_initiative
