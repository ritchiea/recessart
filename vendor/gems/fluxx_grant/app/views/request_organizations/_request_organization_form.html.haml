= form.input :request_id, :as => :hidden

= render :partial => "organizations/single_org_lookup", :locals => { :model => model, :form => form }

= form.input :description, :as => :text

= form.commit_button
