%h2
  = model.first_name
  = model.last_name
%h4
  = model.primary_user_organization.title if model.primary_user_organization
%p
  = model.primary_user_organization.organization_name if model.primary_user_organization