= form.input :taskable_id, :as => :hidden
= form.input :taskable_type, :as => :hidden

= form.input :task_text, :label => "Task Text:", :as => :text
-# Need to have a way to get a list of related users if the entity supports that
= form.input :assigned_user, :label => "Assign Task To:", :as => :autocomplete, :autocomplete_url => users_path(:format => :autocomplete), :related_attribute_name => :full_name
= form.input :due_at, :label => "Date Due:"

= form.commit_button
