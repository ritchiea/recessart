= form.input :project_id, :as => :hidden

= form.input :user, :label => "Person:", :as => :autocomplete, :autocomplete_url => users_path(:format => :autocomplete), :related_attribute_name => :full_name
= form.input :description, :as => :text

= form.commit_button
